# For the proxy service (set in Render dashboard, NOT committed):
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME
PROXY_TOKEN=<openssl rand -hex 32>

# For local recon scripts that talk to the deployed proxy:
PROXY_URL=https://pg-recon-proxy.onrender.com
# PROXY_TOKEN= (same value as on the proxy)
